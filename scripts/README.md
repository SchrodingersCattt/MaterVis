# MatterVis scripts

Use this directory for project-local analysis and figure-generation scripts that
are not part of the public `examples/` API. Keep private or unpublished CIF data
under `scripts/_data/`; that path is ignored by git.

Public, runnable examples that document supported APIs should remain under
`examples/`.
