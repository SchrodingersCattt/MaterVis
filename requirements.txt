dash
plotly
numpy
scipy
gemmi
pymatgen
kaleido
flask-sock
# brotli + gzip for /_dash-update-component responses. The Mesh3d
# vertex base64 (and our 1 MB-per-frame topology figure) compresses
# ~5x; without this every Labels/Axes click ships a megabyte over
# the wire and feels like a 5-10 s lag on consumer connections.
flask-compress
matplotlib
pillow
ase
networkx
# Molecule identification, disorder handling, and per-formula-unit stoichiometry
# (GCD across cell-counts) for the formula_unit display mode. Hard dep --
# without it the formula_unit picker falls back to legacy heuristics that
# get the anion count wrong on perchlorate salts (DAP-4 surfaces 4 ClO4
# instead of 3, etc.).  See crystal_viewer/molcrys_bridge.py.
molcrys-kit
